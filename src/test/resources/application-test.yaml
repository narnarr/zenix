spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:mysql:8.0://localhost/zenix
    username: root
    password:

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop